还有类似的吗 cat -v
以
其实 ^['忽略'了进制 反而更

可以拆分为两个程序

一个转为 所谓的 人为定义的标准形式

cat -v 夸赞

---


在可视编辑下 回车其实是可视的
在行编辑下 回车几乎是不可视的
---
发送编码和接受编码

---

终端没有空格的概念？
或者都是空格

打字机没有空格

空格真的是space

---

但是程序的缓冲区
如果初始化是空格的话
在打印的时候的就可以不用检测是否是空字符，直接打印即可

_若有若无有的空格

或者说 显示取决于本地的配置
控制发送 取决于host的配置

但是 vim 为什么要响应host的配置啊

---

不能是'单行'匹配 替换
光标会移动

ctrl
和shift都不发送信号
esc会发送

---

怎么这么
怎么怎么
不能更改导入的变量
为什么ABCD要声明全局 为什么

-------

不是assert
是视觉检查 吗


---
mirror ?